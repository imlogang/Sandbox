import type { SearchResultsRenderState } from '../lib/useSearchResults';
import type { SearchStateRenderState } from '../lib/useSearchState';
import type { InstantSearch, Middleware, UiState } from 'instantsearch.js';
declare type InstantSearchApi<TUiState extends UiState> = SearchStateRenderState<TUiState> & SearchResultsRenderState & {
    use: (...middlewares: Middleware[]) => () => void;
    refresh: InstantSearch['refresh'];
};
export declare function useInstantSearch<TUiState extends UiState = UiState>(): InstantSearchApi<TUiState>;
export {};
