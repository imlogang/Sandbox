version: 2.1
orbs:
  android: circleci/android@1.0.3
  docker: circleci/docker@1.4.0

workflows:
  docker-image-test:
    jobs:
      - testing-that-one-docker-image

jobs:
  testing-that-one-docker-image:
    docker:
      - image: cimg/php:8.1-browsers
    resource_class: medium
    steps:
      - run: sudo apt-get update 
      - run: sudo apt-get install php8.1-ldap

      
#version: 2.1
# ro
#
#jobs:
#  runner:
#    machine: true
#    resource_class: logan/medium
#    steps:
#      - run: echo "Hi I'm on Runners!"
#
#workflows:
#  testing:
#    jobs:
#      - runner
