version: 2.1

default_test_steps: &default_test_steps
  docker:
    - image: circleci/node:14-browsers
  steps:
    - checkout
    - run: npm ci

jobs:
  test-1:
    <<: *default_test_steps
    steps:
        npx percy exec -- npm run test


workflows:
  test_and_release:
    jobs:
      - test-1