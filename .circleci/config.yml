version: 2.1
workflows:
  btd:
    jobs:
      - deploy
jobs:
  deploy:
    docker:
      - image: cimg/base:2022.05-22.04
    steps:
      - run: apt list at
      - run: sudo apt-get update -y && sudo apt-get install at


#version: 2.1
# ro
#
#jobs:
#  runner:
#    machine: true
#    resource_class: logan/medium
#    steps:
#      - run: echo "Hi I'm on Runners!"
#
#workflows:
#  testing:
#    jobs:
#      - runner
