version: 2.1
orbs:
  python: circleci/python@1.0.0

jobs:
  build:
    machine: true
    resource_class: logan/support-class
    steps:
      - run: nproc

workflows:
  build-workflow:
    jobs:
      - build
      - testing


#version: 2.1
# ro
#
#jobs:
#  runner:
#    machine: true
#    resource_class: logan/medium
#    steps:
#      - run: echo "Hi I'm on Runners!"
#
#workflows:
#  testing:
#    jobs:
#      - runner
