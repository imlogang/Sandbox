version: 2.1

jobs:
  build-docker:
    docker:
      - image: cimg/node:17.2.0
    steps:
      - run: echo "docker image"
  build-windows:
    executor: win/default
    steps:
      - run: echo "windows image"
  build-macos:
    xcode: 12.5.1
    steps:
      - run: xcodebuild --version

workflows:
  build-docker-workflow:
    jobs:
      - build-docker
      - build-windows
      - build-macos