version: 2
workflows:
  btd:
    jobs:
      - deploy
jobs:
  deploy:
    docker:
      - image: cimg/base:2022.05-22.04
    steps:
      - run: echo "this is a test"
      - command: | 
              curl -u ${CIRCLE_API_USER_TOKEN}: \
              -d "build_parameters[CIRCLE_JOB]=testing" \
              https://app.circleci.com/pipelines/github/imlogang/Sandbox?branch=main&filter=all
  testing:
    docker:
      - image: cimg/base:2022.05-22.04
    steps:
      - run: "This was run via curl"

#version: 2.1
# ro
#
#jobs:
#  runner:
#    machine: true
#    resource_class: logan/medium
#    steps:
#      - run: echo "Hi I'm on Runners!"
#
#workflows:
#  testing:
#    jobs:
#      - runner
