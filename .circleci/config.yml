version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2022.04
    steps:
      - run: sudo dmidecode -t processor | grep -E '(Core Count|Thread Count)'


workflows:
  build-workflow:
    jobs:
      - build

#version: 2.1
# ro
#
#jobs:
#  runner:
#    machine: true
#    resource_class: logan/medium
#    steps:
#      - run: echo "Hi I'm on Runners!"
#
#workflows:
#  testing:
#    jobs:
#      - runner
